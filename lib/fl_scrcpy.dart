import 'package:flutter/material.dart';

class FlScrcpy extends StatelessWidget {
  const FlScrcpy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
