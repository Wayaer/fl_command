name: fl_scrcpy
description: A new Flutter project.
version: 0.0.1
repository: https://github.com/Wayaer/fl_scrcpy.git

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  process_run: ^0.12.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter: